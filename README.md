# ai_task1
first task in ai
image recognition model
I began by uploading images as a training set for two different classes (dogs and cats), consisting of five images per class.
 then trained the model to recognize dog features and distinguish them from cats.
 Afterward, I uploaded the code to Google Colab to test the model's accuracy using an image for the test set
 the results showed an accuracy of approximately 0.99, demonstrating that the model operates with high precision. 
